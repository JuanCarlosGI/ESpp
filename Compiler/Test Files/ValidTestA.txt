variables:
	entero uno, dos;
	cadena tres;
	booleano cuatro;
	decimal cinco;
	entero[75] seis; 

funciones:
	rutina primera(entero uno, cadena dos) { }
	entero segunda() { }

main {
	uno = 3 + 4 * 5 / 7 % 2 * (3 + 5) - uno - (-dos) + segunda() - leer() * aleatorio();
	entero local;
	local = 0;
	primera(uno, 3*5);
	imprimir(uno, dos, 5 * 9);
	si (uno) {
		si (dos) {
		}
	}

	mientras (verdadero) {
		imrpimir("verdadero");
	}
}